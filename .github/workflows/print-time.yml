name: Print Current Time22

on:
  # Run every day at 8 AM
  schedule: 
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

jobs:
  print-time:
    runs-on: ubuntu-latest
    steps:
      - name: Print current time
        run: echo "The current time is $(date)"
